# 数据库配置
# SQLite (默认)
DATABASE_TYPE=sqlite
DATABASE_URL=sqlite:///./peiyin.db

# MySQL (取消注释以下行使用MySQL)
# DATABASE_TYPE=mysql
# DATABASE_HOST=localhost
# DATABASE_PORT=3306
# DATABASE_NAME=peiyin
# DATABASE_USER=root
# DATABASE_PASSWORD=your_password

# 如果使用MySQL，需要安装 pymysql: pip install pymysql
# DATABASE_URL 会自动根据上述配置生成，也可以直接指定完整URL:
# DATABASE_URL=mysql+pymysql://user:password@localhost:3306/peiyin

# Vosk 远程评分服务配置
VOSK_SERVICE_URL=https://vosk.coding61.com/score
VOSK_TIMEOUT=60
